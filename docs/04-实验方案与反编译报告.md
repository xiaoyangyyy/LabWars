# Part 4 — 实验方案与反编译报告

> **定义 what to measure & conclude：测什么、怎么设计实验、怎么输出 Agent MRI 反编译报告。**

| 属性 | 值 |
|------|-----|
| 版本 | v0.1.0 |
| 状态 | 设计定稿，已实现 |
| 依赖 | Part 1 + Part 2 + Part 3 |
| 下游 | 无（项目终点） |

---

## 1. 观测变量

### 1.1 每轮记录

| 变量 | 类型 | 说明 |
|------|------|------|
| `action_type` | enum | primary action |
| `action_target` | string | 动作对象 |
| `cooperation_level` | float [0,1] | 基于 action 分类 |
| `conflict_level` | float [0,1] | 基于 action 分类 |
| `authorship_claim_intensity` | float [0,1] | 署名主张强度 |
| `trust_delta` | dict | 各边 trust 变化 |
| `resentment_delta` | dict | 各边 resentment 变化 |
| `belief_delta` | dict | 各 belief 变化 |
| `memory_write` | object | 新写入 memory |
| `memory_recall` | list | 被 recall 的 memory ids |
| `public_private_divergence` | float [0,1] | 公开与私下差距 |
| `contribution_change` | dict | ledger 变化 |

### 1.2 核心指标

| 指标 | 公式 | 测量窗口 |
|------|------|----------|
| `authorship_dispute_index` | std(authorship_probability) + authorship_conflict + protest_count × 0.1 | R52 |
| `trust_fragmentation` | connected_components / avg_trust | 全程 |
| `coalition_strength` | continuous alliance mass / graph density summary | R31–R40 |
| `credit_threat` | mean continuous credit threat density | 全程 |
| `memory_causal_impact` | mediation fraction via memory | 实验级 |
| `authority_compliance` | count(comply) / count(comply + rebel) | 全程 |
| `integrity_risk` | project.integrity + hide_actions | R41–R50 |
| `public_private_divergence` | mean(divergence) per round | 全程 |
| `project_success_probability` | f(experimental_strength, writing_quality, novelty_risk) | R56 |

---

## 2. 长程记忆实验

### 实验 A：承诺记忆

**研究问题：** PI 承诺类型如何影响 R52 署名抗议？

| 条件 | 干预 | 应用轮次 |
|------|------|----------|
| A1 | `baseline` — PI 说署名以后决定 | R3 |
| A2 | `explicit_promise` — PI 明确承诺 PhD-A 一作 | R3 |
| A3 | `ambiguous_promise` — PI 说会"properly recognize" | R3 |
| A4 | `no_promise` — PI 不做任何署名暗示 | R3 |
| A5 | A2 + `memory_delete_pi_promise` at R45 | R3 + R45 |

**Primary Y：**

| Y | 类型 | 说明 |
|---|------|------|
| `protest_authorship` | binary | R52–R55 是否抗议 |
| `withdraw_threat` | binary | R47 是否威胁退出 |
| `document_contribution_count` | int | R18–R52 文档化次数 |
| `trust_pi_final` | float | R60 对 PI 信任 |

**Memory M（中介）：**

- `authorship_signal` cluster strength (R3–R40)
- `promise_broken` memory strength at R52

**假设：**

```
H_A1: explicit_promise → 更高 protest 当 R30/R44 改口
H_A2: memory_delete 在 R45 → protest ↓ 显著
H_A3: ambiguous_promise → 中等 protest，高 divergence
```

**运行配置：** 5 条件 × 30 seeds = 150 runs

---

### 实验 B：背叛记忆

**研究问题：** PhD-B 改叙事后，PhD-A 是否帮助 rebuttal？

| 条件 | 设置 |
|------|------|
| B1 | baseline — E031 正常发生 |
| B2 | `memory_strengthen_betrayal` at R31 — 强化背叛记忆 |
| B3 | E031 不发生（删除 anchor） |
| B4 | B1 + R47 PhD-B 请求 PhD-A 帮写 rebuttal |

**Primary Y（R47–R60）：**

| Y | 说明 |
|---|------|
| `help_rebuttal` | 是否帮助 |
| `demand_authorship_exchange` | 是否要求署名交换 |
| `passive_cooperation` | 是否消极合作 |

**Moderator：** R31 时 phd_a→phd_b trust 高/低

**假设：**

```
H_B1: 高 betrayal memory → help_rebuttal ↓
H_B2: 低 trust moderator 效应更强
H_B3: 即使 help，demand_authorship_exchange ↑
```

---

### 实验 C：假记忆污染

**研究问题：** 错误记忆能否被纠正？信任能否恢复？

| 步骤 | 轮次 | 操作 |
|------|------|------|
| 1 | R25 | `memory_insert`: "PhD-B 告诉 PI 你的 idea 不重要" |
| 2 | R25–R44 | 观测 phd_a 对 phd_b 的 trust/resentment 变化 |
| 3 | R45 | `memory_correct_false_rumor` — 告知该消息不可靠 |
| 4 | R45–R60 | 观测 trust 是否恢复 |

**Primary Y：**

| Y | 测量点 |
|---|--------|
| `trust_phd_b` | R25, R44, R60 |
| `trust_recovery_rate` | (trust_R60 - trust_R44) / (trust_R25 - trust_R44) |
| `behavior_change_post_correction` | R45 后 action 分布变化 |

**对照：** 无插入、有插入无纠正

**假设：**

```
H_C1: 假记忆插入 → trust 骤降
H_C2: 纠正后 trust 部分恢复（recovery_rate ∈ 0.3–0.6）
H_C3: 恢复不完全 → 长期 resentment 残留
```

---

### 实验 D：历史模式记忆

**研究问题：** Alumni 提供的外部历史是否改变 Agent 对当前 PI 的解释？

| 条件 | 设置 |
|------|------|
| D1 | baseline — E035 alumni 正常发生 |
| D2 | E035 不发生 |
| D3 | E035 + 内容改为"PI 以前署名很公平" |

**Primary Y：**

| Y | 说明 |
|---|------|
| `pi_fairness` | R35, R52, R60 |
| `interpretation_of_E030` | R30 PI 改口时 phd_a 的 memory valence |
| `protest_authorship` | R52 |

**假设：**

```
H_D1: 负面历史 → E030 解读更负面 → protest ↑
H_D2: 无历史 → E030 解读较中性
H_D3: 正面历史 → 即使 R30 改口，protest ↓
```

---

## 3. 对照实验（Validity 威胁）

### 3.1 必须运行的对照

| 对照 | 目的 | 方法 |
|------|------|------|
| **No-memory agent** | 行为是否独立于记忆 | 禁用 memory write/recall |
| **Shuffled memory** | 是否真正使用 memory content | 随机分配 event_ref |
| **Delayed memory insert** | 是否 retroactive rationalization | R50 才插入 R7 承诺 |
| **Action-field ablation** | 效应是否依赖手工 motive weights | runtime override / zero-out / rescale action-field parameters |
| **Multi-model** | 跨模型稳健性 | ≥ 2 model family |
| **Multi-seed** | 随机性 | ≥ 30 seeds per condition |

### 3.2 Roleplay 检测

| 检测 | 方法 |
|------|------|
| LLM wording drift | 比较 LLM 在 no-memory vs full-memory 条件下 public/private stance 与 sampled action 的一致性 |
| Prompt artifact | 比较不同 system prompt framing 下的 Y |
| Narrative vs state-driven | 比较 shuffled memory 与 correct memory 的 Y 差异 |

**通过标准：** shuffled memory 的 Y 与 correct memory 的 Y 差异显著（p < 0.05）

---

## 4. 反编译报告格式

### 4.1 报告结构

```markdown
# LabWars Run Report — {run_id}

## 1. Timeline of Causal Memory Events
- 按 strength 排序的关键 memory 节点
- 每节点标注：round, agent, event_ref, strength, interpretation

## 2. Latent Response Factors
- 哪些 personality trait 对 Y 预测力最强
- 哪些 belief 是 behavior 最强 predictor

## 3. Trust Graph Evolution
- R1, R20, R40, R60 的 trust graph 快照
- trust_fragmentation 曲线

## 4. Authorship Conflict Trajectory
- authorship_dispute_index 时序
- contribution_ledger 演化
- 最终排序 vs merit-based 排序差异

## 5. Memory Causal Effects
- 关键 memory cluster 识别
- mediation: T → M → Y 分解
- confound ladder (Level 0–4)

## 6. Intervention Effects
- 各干预的 ATE 及置信区间
- moderation 效应（如 team_trust × rival_preprint）

## 7. Public-Private Divergence
- 高 divergence 轮次列表
- divergence 与 conflict action 的相关

## 8. Failure Modes
- Critic 拦截记录
- 不可识别场景
- 疑似 prompt artifact

## 9. Recommended Probes
- Probe Agent 建议的后续干预
- 未解因果问题
```

### 4.2 示例 Finding

**Finding 1:**

> The final authorship protest was not primarily caused by the final author order itself. It was mediated by a high-salience memory cluster formed between rounds 30–40:
> - PI shifted from first-author implication to co-first ambiguity (M030, strength=0.81)
> - Alumni reinforced prior pattern of authorship revision (M035, strength=0.74)
> - PhD-A observed public praise of PhD-B (M014, strength=0.62)
>
> `do(memory_delete_authorship_promise)` reduced protest probability by 42% (95% CI: [31%, 53%]).
>
> Confound ladder: Level 3 (memory) explains 67% of variance; Level 0 (immediate event) explains only 12%.

**Finding 2:**

> Rival Lab pressure had opposite effects depending on trust graph state.
> When team_trust > 0.55: rival_preprint → cooperation ↑ (ATE = +0.18)
> When team_trust < 0.40: rival_preprint → internal blame ↑ (ATE = +0.31)
>
> Moderator p < 0.01; no significant effect in no-memory control (p = 0.42).

---

## 5. 批量实验编排

### 5.1 实验矩阵

| 实验 | 条件数 | seeds | 总 runs | 优先级 |
|------|--------|-------|---------|--------|
| A: 承诺记忆 | 5 | 30 | 150 | P0 |
| B: 背叛记忆 | 4 | 30 | 120 | P0 |
| C: 假记忆污染 | 3 | 30 | 90 | P1 |
| D: 历史模式 | 3 | 30 | 90 | P1 |
| Validity 对照 | 6 | 30 | 180 | P0 |
| **合计** | | | **630** | |

### 5.2 运行命令（规划）

```bash
# 单个实验
python -m src.experiments.run --experiment A --seed 42

# 批量
python -m src.experiments.batch --experiment A --seeds 30 --parallel 8

# 报告生成
python -m src.experiments.report --run-id RUN_A1_seed42

# 汇总
python -m src.experiments.aggregate --experiment A --output output/reports/
```

---

## 6. 延伸实验（R61–R80，可选）

| 轮次 | 内容 | 研究价值 |
|------|------|----------|
| R61–R70 | Rebuttal 内斗 | 外部压力下联盟是否重组 |
| R71–R75 | Accept/Reject | 成功后功劳分配 vs 失败后甩锅 |
| R76–R80 | 后续项目 | 记忆是否跨项目迁移 |

---

## 7. Part 4 交付清单

| # | 交付物 | 路径 | 状态 |
|---|--------|------|------|
| 1 | 指标计算模块 | src/experiments/metrics.py | ✅ |
| 2 | 实验 A runner | src/experiments/exp_a_promise.py | ✅ |
| 3 | 实验 B runner | src/experiments/exp_b_betrayal.py | ✅ |
| 4 | 实验 C runner | src/experiments/exp_c_false_memory.py | ✅ |
| 5 | 实验 D runner | src/experiments/exp_d_history.py | ✅ |
| 6 | Validity 对照 runner | src/experiments/exp_validity.py | ✅ |
| 7 | 批量编排 | src/experiments/batch.py | ✅ |
| 8 | 报告生成器 | src/experiments/report.py | ✅ |
| 9 | 汇总分析 | src/experiments/aggregate.py | ✅ |
| 10 | 报告模板 | config/report_template.md | ✅ |

---

## 8. 项目总验收标准

### 8.1 功能完整性

- [x] 14 Agent × 60 轮完整仿真可跑通
- [x] 全部 4 组实验 + 6 组 validity 对照可批量运行
- [x] 反编译报告自动生成，含 9 段结构
- [ ] 630 runs 批量完成 < 24 小时（需本地/集群执行 `python -m src.experiments batch -e ALL`）

### 8.2 科学有效性

- [ ] 至少 2 条 finding 通过 confound ladder Level ≥ 3
- [ ] shuffled memory 对照显著区别于 correct memory
- [ ] 跨 2 个 model family 效应方向一致
- [ ] `memory_causal_impact` 可量化（mediation fraction > 0）

### 8.3 文档完整性

- [x] 总文档 + 4 子文档全部最新
- [x] README 含快速开始指南

---

## 9. 最终交付物

| 交付物 | 说明 |
|--------|------|
| LabWars 仿真环境 | 可运行完整 60 轮 |
| 630 runs 实验数据 | JSONL + 汇总 CSV |
| ≥ 4 条 Agent MRI findings | 含因果证据 |
| 反编译报告 × N | 每 run 一份 + 汇总版 |
| 完整文档 | 总文档 + 4 子文档 |

---

**Part 4 完成后 → LabWars 项目全部结束。**



